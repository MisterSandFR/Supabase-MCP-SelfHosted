runtime: python
version: 1.0.0
description: "Serveur MCP Supabase minimal pour test"

build:
  command: "echo 'Build minimal - pas de dépendances'"

server:
  command: "python src/supabase_server.py"

env:
  SUPABASE_URL:
    description: "URL Supabase"
    required: true