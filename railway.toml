[build]
builder = "dockerfile"

[deploy]
startCommand = "python src/supabase_server_simple.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "always"

[env]
PORT = "8000"
PYTHONUNBUFFERED = "1"