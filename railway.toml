[build]
builder = "dockerfile"

[deploy]
startCommand = "python mcp_hub.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "always"

[env]
PORT = "8000"
PYTHONUNBUFFERED = "1"