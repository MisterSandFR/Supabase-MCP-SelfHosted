# Apify-specific Dockerfile
FROM apify/actor-node:18

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev for TypeScript build)
RUN npm ci || npm install

# Copy source files
COPY . ./

# Build TypeScript
RUN npm run build

# Run the Apify actor using npm script
CMD ["npm", "run", "start:apify"]